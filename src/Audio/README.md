# Route music assets (FEATURE-MAP-087)

Place looping route music files here as `*.ogg` (e.g. `route_1.ogg`).

The Audio Engine editor and `musicTrack` map field reference stems without the extension.
